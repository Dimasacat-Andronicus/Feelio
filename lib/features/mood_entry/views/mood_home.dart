import 'package:flutter/material.dart';

class MoodHome extends StatelessWidget {
  const MoodHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      child: Center(child: Text('Mood Home')),
    );
  }
}
