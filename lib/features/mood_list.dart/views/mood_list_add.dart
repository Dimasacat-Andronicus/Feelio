import 'package:flutter/material.dart';

class MoodListAdd extends StatelessWidget {
  const MoodListAdd({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text('Mood List Add', style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
